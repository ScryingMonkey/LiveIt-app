export class User {
    public displayName:string
    public email:string
    public userHasPicture:boolean
    public photoURL:string
    public providerId:string
    public uid: string
    public userType: string

constructor(displayName, email, userHasPicture, photoURL, providerId, uid, userType){}
}